import { style } from 'typestyle';

export const Container = style({
  display: 'flex',
  flexDirection: 'column',
  height: '100%',
});

export const Body = style({
  padding: '35px',
  display: 'flex',
  height: '100%',
  width: '100%',
});
