import { style } from 'typestyle';

export const Container = style({
  display: 'flex',
  justifyContent: 'center',
  flexDirection: 'column',
  marginTop: '75px',
  width: '100%',
});

export const TrustedAddress = style({
  fontSize: '24px',
  fontWeight: '600',
});
